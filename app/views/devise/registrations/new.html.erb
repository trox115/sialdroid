<div class="row">
  <div class="col-lg-4 col-md-6 ml-auto mr-auto">
    <h1 class="text-center">Registar</h1>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.text_field :name, autofocus: true, class: 'form-control', placeholder: "Nome" %>
      </div>

      <div class="form-group">
        <%= f.email_field :email, autofocus: false, class: 'form-control', placeholder: "Endereço de Email" ,id:"email"%>
      </div>

      <div class="form-group">
        <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password' %>
      </div>

      <div class="form-group">
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'Confirme a Password', id: "Registo"%>
      </div>

      <div class="form-group">

        <%= f.text_field :companies_id, autocomplete: "off", class: 'form-control', placeholder: 'email', value:"1",id:"empresa", :disabled => false%>
      </div>

      <div class="form-group">
        <%= f.submit "Registar", class: "btn btn-primary btn-block btn-lg" %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
<script type="text/javascript">
  document.getElementById("Registo").addEventListener("click", function(){
    a=document.getElementById("email").value
    var empresa = a.replace(/.*@/, "");
    empresa=empresa.split('.');
    empresa=empresa[0]
    empresa = empresa.replace(/^./, empresa[0].toUpperCase());
    document.getElementById("empresa").value=empresa


});


</script>
